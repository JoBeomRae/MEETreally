import 'package:flutter/material.dart';

class InNow extends StatelessWidget {
  const InNow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('나우입니다. 반갑습니다.'),
    );
  }
}
