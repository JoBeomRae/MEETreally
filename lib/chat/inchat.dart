import 'package:flutter/material.dart';

class InChat extends StatelessWidget {
  const InChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('채팅 내용'));
  }
}
