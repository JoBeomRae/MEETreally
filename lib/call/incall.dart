import 'package:flutter/material.dart';

class InCall extends StatelessWidget {
  const InCall({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('전화내역 내용'));
  }
}
